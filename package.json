{
  "name": "literallifechurch",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "twilio-run deploy",
    "start": "twilio-run",
    "tunnel": "twilio-run --ngrok"
  },
  "dependencies": {
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
